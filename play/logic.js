/* The Island Explorer is a fully client-side game — all rules live in index.html.
   This module exists to satisfy the packaging contract. */
export function init() { return {}; }
export function turn(state) { return state; }
