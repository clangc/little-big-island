# The Little Big Island â marketing site

Static site. `index.html` = founding-family landing page. `island-finder/` = the free quiz.

## Deploy (Vercel)
1. Import this repo at vercel.com/new â framework preset: "Other", no build command, output = root.
2. Add domain thelittlebigisland.com in Vercel â Project â Settings â Domains, then set your registrar's DNS as Vercel instructs.

## Before launch
- In index.html: add the founder photo (search `founder-photo`) and paste the Stripe payment link (search `PAYMENT_LINK_URL`).

<!-- deploy nudge 76ff5e99 -->
